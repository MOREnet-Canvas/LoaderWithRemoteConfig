## Canvas Remote Config Test

### What to upload to Canvas Theme
Upload `public/loader.js` to Theme Editor JS.

### Where config lives
GitHub Pages hosts:
- /config/<canvas-domain>.json
- /dist/app.js

Loader chooses config by `location.hostname`.